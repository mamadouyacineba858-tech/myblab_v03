# MB-VIS-LED-V19

Visual continuation of the LED renderer work.

Scope: LedPart.jsx only.

Goal: increase the straight anode lead visual diameter while preserving the electrical endpoint `(28,62)` and the existing cathode geometry. No changes to component definitions, pins, Breadboard, simulation, or CircuitComponent.
