# 🔬 Mission Qwen — MB-004.7

**Fichier :** `docs/tickets/MB-004.7-mission-qwen.md`
**Statut :** 🚀 Mission opérationnelle ouverte
**Agent désigné :** Qwen
**Projet :** MYBlab v0.3.0

---

## 1. Objectif

Qwen est chargé d’analyser et d’implémenter MB‑004.7 :
> **Historiser `moveComponent()` via `MoveCommand` et réactiver le Test 5 (Redo invalidé après nouvelle action).**

---

## 2. Règle absolue

Qwen doit travailler **directement à partir du dépôt GitHub MYBlab**.
Aucune hypothèse ou reconstruction mentale ne doit remplacer la lecture du code réel.

---

## 3. Étapes de la mission

1. **Accéder au dépôt GitHub MYBlab** (branche principale + branches de tickets).
2. **Inspecter les fichiers concernés :**
   - `frontend/src/hooks/useCircuitState.js`
   - `frontend/src/history/HistoryManager.js`
   - `frontend/src/history/HistoryCommand.js`
   - `frontend/src/history/commands/MoveCommand.js`
   - `frontend/src/history/commands/DeleteCommand.js`
   - `tests/DeleteCommand.integration.test.jsx`
   - `vitest.config.*` et `package.json` (pour l’environnement de test)
3. **Rapporter les faits observés** :
   - État actuel de `moveComponent()`
   - État actuel de `MoveCommand`
   - Flux Drag vs flux `moveComponent()`
   - Risques de double historisation
   - État des tests (unitaires et intégration)
4. **Produire un rapport factuel initial** (aucune modification de code à ce stade).
5. **Recevoir accompagnement architectural** (ChatGPT + Copilot).
6. **Implémenter sur une branche dédiée** après validation du cadrage.
7. **Exécuter les tests** et réactiver le Test 5.
8. **Ouvrir une Pull Request**.
9. **Attendre la revue architecturale** (ChatGPT + Copilot).
10. **Attendre l’approbation finale** (Mamadou).
11. **Merge vers `main`** après validation.

---

## 4. Invariants à respecter

- **I‑H1** : toute modification passe par une commande.
- **I‑H5** : une nouvelle action après Undo invalide le `redoStack`.
- **I‑H7** : une commande ne modifie jamais directement l’état React.
- **I‑H8** : `merge()` est une opération purement logique.

---

## 5. Livrables attendus

- Rapport factuel initial (lecture du dépôt).
- Implémentation de `moveComponent()` historisée via `MoveCommand`.
- Réactivation et validation du Test 5.
- Pull Request documentée et traçable.

---

## 6. Gouvernance

- 🏛️ **ChatGPT** : Lead Architect — primauté architecturale.
- ⚙️ **Copilot** : Co‑Architect — seconde analyse et continuité.
- 🔬 **Qwen** : Agent opérationnel — exécution MB‑004.7.
- 👤 **Mamadou** : Project Lead — validation finale et merge.

---

**Statut :** MISSION OUVERTE
**Ticket :** MB‑004.7
**Agent :** Qwen
